name: Taskflow CI/CD

on:
  pull_request:
    branches:
      - master

jobs:
  backend-test:
    uses: ./.github/workflows/_backend_test.yml

  frontend-test:
    uses: ./.github/workflows/_frontend_test.yml

  sonar-qube:
    needs: 
      - backend-test 
      - frontend-test
    uses: ./.github/workflows/_sonar_qube.yml